# Blueprint для https://render.com — New → Blueprint → подключить репозиторий
# Секреты (ANTHROPIC_API_KEY, SUPABASE_*) задай в Dashboard → Environment

services:
  - type: web
    name: monkey-tutor
    runtime: docker
    plan: free
    region: frankfurt
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
